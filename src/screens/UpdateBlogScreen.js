import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const UpdateBlogScreen = () => {
  return (
    <View>
      <Text>Update Blog Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default UpdateBlogScreen;
